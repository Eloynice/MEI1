//aula 1 
//forks puxa mais que threads
//no apache ele não vai criando os fork20 os(processos), cria logo 20 no inicio
sum = 0;
for(i = 1; i <= 1000000; i += 2) {
	sum = sum + sqrt(i);
	}
return sum;

#include <stdio.h>
#include <math.h>

#define MAX 1000000000
int main()
{
  long double i;
  long double sum = 0;

  for (i = 1; i <= MAX; i++) {
    sum = sum + sqrtl(i);
    //printf("%Lf\n", sum);
  }

  printf("SUM: %Lf\n", sum);
}
